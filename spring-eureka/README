1. Go to spring-eureka-service
2. Run command:
    mvn spring-boot:run
3. Eureka server is available on http://localhost:8761
4. Go to spring-eureka-client
5. Run command:
    mvn spring-boot:run
6. Check both Eureka server (http://localhost:8761) and client endpoint (http://localhost:9002/service-instances/client-application)